# mypackage

This library was created as an example of how to write your own pthon package and push it to github for downloading or use by others. The package is a bubble sort algorithm that sorts a given list or array in descending order and retursn the top n items.
example:
in the array below my_array = np.array[9, 10, 13, 6, 8, 4, 1, 2, 0]
top_n([my_array, 3]) would return [13, 10, 9]

## how to install

head to http://github.com/alfred_142 and install all the packages in the my package directrory

....readme.md is a description of our product
